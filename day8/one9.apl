instrs ← ('□' 3 2) ('↕' 2 1) ('↔' 1 4) ('↕' 2 1)

grid ← 3 7 ⍴ 0

apply ← {
    (⊃⍵) ≡ '□' : (_ w h ← ⍵ ⋄ grid[⍳h; ⍳w] ← 1) ⋄
    (⊃⍵) ≡ '↕' : (_ x mag ← ⍵ ⋄ grid[;x] ← (-mag) ⊖ grid[;x]) ⋄
    (⊃⍵) ≡ '↔' : (_ y mag ← ⍵ ⋄ grid[y;] ← (-mag) ⌽ grid[y;]) ⋄
}

⎕ ← +/ +/ grid ⊣ apply¨ instrs
